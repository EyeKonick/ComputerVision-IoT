Type: task
Status: open
Blocked by: 03

## Question

Get the built Hand Tracking guide (ticket 03) live on Vercel via GitHub integration.

Covers:
- Confirming/creating a GitHub repository for this project (ask the instructor for their GitHub account/org and desired repo visibility — not something to guess)
- Pushing this repo's history to GitHub (currently has no remote configured)
- Connecting the GitHub repo to Vercel (dashboard step the instructor drives, or CLI-assisted — confirm which) and running the first deploy
- Verifying the live URL actually works end-to-end (loads, step-chain navigation works, responsive on a phone-sized viewport)
- Confirming future pushes to the main branch auto-deploy, so continuing Face Tracking later (on the sibling map) is a simple git push

This is a HITL task — the instructor's GitHub/Vercel accounts and choices are needed, not something the agent can decide alone. Resolved when a real, working public URL exists and is confirmed live.
