# Map: CV/IoT Student Guide App — Build & Deploy

## Destination

Both tracks — **Hand Tracking** (Setup & Environment + Sessions 1–4, fully built) and **Facial Recognition** (Sessions 1–2 built; Sessions 3–4 land as their own tickets once each one's content closes on the sibling map) — built as real, responsive pages in this Next.js app, using a Fable-designed visual direction (currently Hand Tracking's own — see Notes) and a structured JSON/TS content model converted from each track's already-drafted markdown, with cross-guide navigation between them, and **deployed live on Vercel** (via GitHub integration) at a public URL students can visit on their own laptop or phone, not just a projector.

**Formerly scoped to Hand Tracking only** — see [Face Session 1 & cross-guide nav](issues/14-face-session-1-and-cross-guide-nav.md) for the scope-extension decision and why. This is still a **narrower, faster-moving sibling** of the [CV/IoT Student Guide map](../cv-iot-student-guide/map.md), not a replacement for it: that map is where each session's teaching *content* gets drafted and instructor-approved before it can be built here; the multi-activity node overview and the general Claude Code + Fable build-prompt stay parked there too.

## Notes

- **"Fast" means narrow scope and priority order, not lower quality.** The user wants Hand Tracking finished and shipped *before* other topics — that's the speed lever (don't wait on Face Tracking or the rest of the course to ship this) — not a license to cut corners on this track itself. The output is a **full, polished product**: real design (via the Fable pass), genuinely working responsive pages, a real deploy. No "basic MVP" framing on any ticket here.
- **This effort carries execution into the tickets** (same override as the sibling map): resolving a ticket here means actually building the thing — pages, components, data, deployment — not just deciding an approach.
- **Source content is final** — do not redraft it. Convert faithfully from the 5 already-drafted, instructor-approved markdown files:
  - `../cv-iot-student-guide/content/01-setup-environment.md`
  - `../cv-iot-student-guide/content/02-hand-session-1-webcam-loop.md`
  - `../cv-iot-student-guide/content/03-hand-session-2-landmarks-skeleton.md`
  - `../cv-iot-student-guide/content/04-hand-session-3-trails-gestures.md`
  - `../cv-iot-student-guide/content/05-hand-session-4-orb-wrapup.md`
- **Visual/navigation metaphor carried over as a given** from the sibling map's earlier grilling (do not re-litigate): a linear step-chain of connected nodes in strict sequence (no branching), next/previous, no in-browser code execution (read-along reference; students code in VS Code). **Superseded for this track:** the "jump-to-any-step overview" part of that given was overturned by direct instructor feedback (see [Sequential lock & single-path fork display](issues/05-sequential-lock-and-fork-display.md)) — steps now unlock strictly in order; a step's node isn't clickable (or reachable by direct URL) until the one before it has been visited.
- Responsive from the start — must work reasonably well on both a laptop/projector and a student's phone.
- Repo has no git remote configured yet and no Vercel config — first deploy requires creating/pushing to a GitHub repo and connecting it in Vercel.
- **Progress tracking: per-device only, via `localStorage`.** Each student's own browser remembers their last-visited step and resumes there next visit — no accounts, no server-side state, nothing synced across devices or visible to the instructor. Compatible with the sibling map's original "no login, no tracking" decision (still true — nothing leaves the device). Confirmed via the Fable mockup, which now demonstrates this live.
- **Per-symbol glossary, added to every step.** Every new built-in function/symbol/concept a step introduces gets its own short, plain-language, non-technical definition — a distinct "New in this step" list, separate from the "why it matters" prose (which stays focused on design rationale, not vocabulary). Demonstrated in the Fable mockup on 5 real steps, then authored across all 26 steps in the sibling map's approved content files (`content/01-setup-environment.md` through `05-hand-session-4-orb-wrapup.md`) — **done**, ready for ticket 02 to transcribe as-is.
- **Code blocks block copy/paste.** Students must type code themselves — the code panel disables text selection and intercepts copy/cut/right-click with a message ("Type it yourself — that's the point"). Demonstrated live in the Fable mockup.
- **Camera-setup fork gets a visual split in the node-chain.** The Setup track's "Set up your camera source" step is one step in the data model (Path A/B are documented together within it, not separate steps) but is rendered as two labeled branches connected by smooth flowing curves (not a hard bracket) that visibly re-merge into the single chain before the next step. The two branches carry **independent visual state**: clicking the path you actually took lights up only that curve+node (cyan for Laptop, amber for Phone), the other stays dimmed — persisted per-device via `localStorage` alongside the rest of progress tracking, so it's not just "which one did I click most recently," it remembers which path is *yours*. Demonstrated live in the Fable mockup. This is the one deliberate exception to the "strictly linear, no branching" navigation rule — it's a visual split *within* one step's content, not an alternate route through the guide; the chain itself never forks.
- Consult `AGENTS.md` at the repo root before touching any Next.js code — this project has non-standard/breaking-change docs under `node_modules/next/dist/docs/` since it's pinned to a pre-release Next.js version.
- If a ticket surfaces a real open decision or ambiguity, invoke `/grilling` and `/domain-modeling` rather than guessing.
- **Troubleshooting sub-pages are in scope**, added for beginner students with zero Python background who hit real setup failures (missing deps, adb/USB-debugging issues, Python permission errors). These live entirely **outside the node-chain** — not steps, never lock-guarded, never a `localStorage` resume target — so they do not reopen the "strictly linear, no branching" chain decision above; see [Troubleshooting UX scope & navigation model](issues/07-troubleshooting-ux-scope.md) for the full decision set. **Hand Tracking only so far** — Facial Recognition has no troubleshooting sub-pages yet (no ticket 08-style research exists for it); a future ticket if it's ever wanted there.
- **Facial Recognition track added to this map's scope** (was previously parked entirely on the sibling map — see [Face Session 1 & cross-guide nav](issues/14-face-session-1-and-cross-guide-nav.md)). Built as a structural mirror of Hand Tracking's own `app/hand-tracking/` tree under `app/facial-recognition/`: separate `data/`/`lib/`/`components/`, its own `localStorage` key (`frg_progress_v1`, independent of Hand Tracking's `htg_progress_v1` — a student's progress in one track never affects the other), same CSS design system with every class prefix renamed `ht-` → `fr-` (no new Fable pass — reuses Hand Tracking's approved visual direction verbatim, not yet reviewed as its own design decision). Each track's chain-rail header carries a "↔ Switch to \<other track\>" link so a student can move between them without losing their place in either.
- **`CAM_SOURCE` personalization reads across tracks, one-way.** Facial Recognition has no camera-source fork of its own (that fork lives once, in Hand Tracking's shared Setup session). Its code panels read Hand Tracking's stored fork choice (`htg_progress_v1`'s `forkChoices["setup-6"]`) read-only, so a student who picked their camera path once during Setup sees it reflected in Facial Recognition's code too, without being asked again. Never written back from the Facial Recognition side.
- **Facial Recognition's glossary pass is still outstanding.** Same gap the sibling map's Notes already flagged: "New in this step" glossary entries were never authored for Face Tracking content. Session 1's steps ship with `glossary: []` rather than guessed entries — real content work needing instructor review, not a structural-build task. See Not yet specified.

## Decisions so far

- [Fable design pass](issues/01-fable-design-pass.md) — approved visual direction: near-black ground with three accents pulled from `hand_ar.py`'s own colors (magenta/cyan/amber), monospace-for-chrome + sans-for-prose typography, and a signature node-chain overview literally styled like a hand-landmark skeleton. Also where progress tracking (`localStorage`), the per-step glossary format, and no-copy code blocks were decided and demonstrated live. Published mockup: `https://claude.ai/code/artifact/0ef32d70-0ff0-4bf9-a42c-4d19748a630d`.
- [Content data model & conversion](issues/02-content-data-model.md) — `Session`/`Step` TypeScript shape built in `app/hand-tracking/data/types.ts`; all 5 source markdown files converted into `sessions/00-setup.ts` … `04-hand-session-4.ts` plus a combining `index.ts` (26 flattened steps total). Step/code-block/common-problem counts verified to match the source markdown exactly; two harmless pre-existing content quirks (not conversion errors) found and flagged in place rather than silently fixed.
- [Build guide pages](issues/03-build-guide-pages.md) — real Next.js pages built under `app/hand-tracking/` (one static route per step, sticky chain-rail layout, camera-fork SVG branches, two-part code panels, no-copy, `localStorage` progress). Ported the approved Fable mockup's CSS near-verbatim for pixel fidelity. Verified live in-browser (desktop + phone viewport); found and fixed a real resume-redirect race-condition bug along the way. One scope note: real syntax highlighting was flagged, not built — the approved mockup never actually showed it, only diff-style new-line highlighting.
- [Sequential lock & single-path fork display](issues/05-sequential-lock-and-fork-display.md) — direct instructor feedback, resolved live: the camera-fork step now shows only the chosen path (a chooser until one is picked), never both at once; and chain nodes now unlock strictly in order (locked nodes are unclickable and un-deep-linkable via a `LockGuard` redirect), overturning the earlier "jump to any step" given. Found and fixed a real race condition where the chain rail was silently legitimizing locked steps before the lock guard could check them.
- [File creation, CAM_SOURCE line, and line context](issues/06-file-creation-camsource-line-context.md) — direct instructor feedback, resolved live: added a "create a new file" callout on the two steps that actually create one (a real ticket 02 transcription gap despite ticket 01 claiming it was fixed); the `CAM_SOURCE` line now shows the student's concrete Path A/B value everywhere it recurs, not a generic both-paths comment; and every "Type this" code group now shows its approximate line range plus the next unchanged line as trailing context, so students can tell a continuation from an insertion.
- [Troubleshooting UX scope & navigation model](issues/07-troubleshooting-ux-scope.md) — nine decisions via live grilling: extends this map (not a new one); dedicated per-symptom/category troubleshooting sub-pages reached via a "Stuck?" link, living entirely outside the node-chain; scoped to the Setup & Environment session primarily (not "Hand Session 1" as literally named); research covers Windows/macOS/Linux plus Android/adb; stays error-recovery-only (no baseline literacy expansion); resume state never targets a troubleshooting page; flat symptom→fix reference style, not an interactive diagnostic; copy-paste allowed on fix commands (unlike main-flow code). Also found a real content gap: no existing coverage of adb bind/listener errors.
- [Research beginner setup & troubleshooting failure scenarios](issues/08-research-troubleshooting-scenarios.md) — 13 Python/pip/venv scenarios + 8 Android/adb/USB scenarios cataloged with sourced symptom/cause/fix, saved to `research/troubleshooting-scenarios.md`. Key findings: mediapipe 0.10.14 only has wheels for CPython 3.9–3.12 (exact cause of the most likely install failure); Gatekeeper/SmartScreen are confirmed non-issues for this course's taught commands; adb bind/listener errors (the confirmed gap) fully diagnosed with real fix commands; charge-only USB cables identified as a previously-undocumented failure mode; DroidCam's "bundles its own adb" claim flagged as unverified for later re-check.
- [Collapsible common-problems accordion](issues/11-collapsible-common-problems-accordion.md) — direct instructor feedback: the inline "Common problems" list on every step (distinct from the ticket 09/10 troubleshooting sub-pages) is now a single-open-at-a-time accordion — `Step.commonProblems` split into `{ error, solution }`, all ~77 entries across all 5 session files converted, new `CommonProblems.tsx` component wired into `StepDetail.tsx`. Confirmed via grilling: no new dedicated "laptop camera" troubleshooting category — camera entries in Setup Steps 6–7 instead got explicit "(Laptop webcam)" / "(Phone over USB)" labels on their (now-collapsed) headline so a student only has to expand entries for their own path. Verified live in-browser at desktop and phone viewport widths.

- [Fix adb forward, not reverse](issues/13-fix-adb-forward-not-reverse.md) — Path B's phone-over-USB tunnel had the direction backwards: `adb reverse` opens its listener on the phone's own port 8080, which IP Webcam's server already occupies, so it failed with "address already in use" every time. Corrected to `adb forward` (plus real debugging-toggle-order and screen-timeout gotchas) across `00-setup.ts`, `01-hand-session-1.ts`, the `adb-bind-listener-error` troubleshooting scenario, both source scripts' `CAM_SOURCE` comments, and the sibling map's source markdown.
- [Face Session 1 & cross-guide nav](issues/14-face-session-1-and-cross-guide-nav.md) — extended this map's scope to include Facial Recognition (decided live, overriding the earlier "after this destination is reached" sequencing note below — see the ticket for the full trade-off). Built `app/facial-recognition/` as a structural mirror of `app/hand-tracking/`: Face Tracking Session 1 (4 steps, transcribed from the sibling map's approved content), own independent `localStorage` progress store, cross-track read-only `CAM_SOURCE` personalization off Hand Tracking's stored Setup-fork choice, and a "↔ Switch to \<other track\>" nav link in both chain rails. Verified live in-browser end to end.
- [Face Session 2 & HUD/mesh build](issues/15-face-session-2-hud-mesh-build.md) — added `data/sessions/02-face-session-2.ts` (4 steps: corner-bracket reticle → scan-line sweep → confidence label → `FaceMesh` overlay). The source markdown never shows steps 2-3 as full cumulative files, and step 4's full listing reveals a new function isn't always appended last (`draw_hud_text`, added in step 3, sits *before* `draw_scan_line`, added in step 2) — reconstructed each step's exact file by inferring the placement that reproduces step 4's confirmed order, then verified all 4 steps' code with `python -m py_compile` (all clean). Also resolved the sibling map's Session 2 content ticket, which was still "claimed" going into this one — content read as complete/instructor-quality already, so building from it and formally resolving it happened together rather than as a separate sign-off pass (flagged explicitly on both tickets).

## Not yet specified

- Exact GitHub account/repo visibility (public vs. private) and Vercel account specifics — resolved when the deploy ticket is actually worked, not blocking the build.
- Exact simplification calibration for [Simplify explanation prose](issues/12-simplify-explanation-prose.md) (open, unclaimed, on the frontier) — scope is settled (all 5 Hand Tracking sessions' step prose + troubleshooting scenario cards) but the target reading level isn't; needs a quick calibration pass against one already-revised step with the instructor before running across the whole track, and likely splits into several per-session tickets once calibrated.
- Facial Recognition Sessions 3–4 (`app/facial-recognition/data/sessions/`) — each lands as its own build ticket here once its content closes on the sibling map (neither is drafted yet: Session 3 covers mood detection, Session 4 covers themes/controls/wrap-up).
- Facial Recognition's own "New in this step" glossary pass — Session 1 shipped with `glossary: []` on every step (see [Face Session 1 & cross-guide nav](issues/14-face-session-1-and-cross-guide-nav.md)); needs the same content-authoring + instructor-review pass Hand Tracking already got, likely on the sibling map first (glossary text is source content) then transcribed here.
- Whether Facial Recognition ever gets its own Fable design pass (a distinct visual identity) or permanently reuses Hand Tracking's approved direction verbatim — not raised as a problem by anyone yet, parked here rather than decided by default.
- Whether Facial Recognition needs troubleshooting sub-pages of its own (ticket 07's system is Hand-Tracking-only right now) — no research/content exists for it yet; revisit if it's ever requested.

## Out of scope

- The cross-activity node overview and the general Claude Code + Fable build-prompt — remain on the [CV/IoT Student Guide map](../cv-iot-student-guide/map.md); that map is still where every session's teaching *content* gets drafted and instructor-approved before it can be built here.
- Anything from that map's own Out of Scope section (other 10 syllabus modules, real face recognition, real IoT actuation) — unchanged, still out of scope here too.
